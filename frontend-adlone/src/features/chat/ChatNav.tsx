// El rail de navegación (chats/favoritos/grupos) se plegó dentro de
// ChatSidebar como tabs, siguiendo el layout de 2 columnas del diseño de
// referencia. Este módulo solo conserva el tipo compartido.
export type ChatView = 'chats' | 'favoritos' | 'grupos';
